# Emoji Mapper runtime dependencies.
# Pure-stdlib scripts (fetch_logos.py) need none of these; the others do.
#
# External (not pip): ffmpeg + ffprobe on PATH are required ONLY for video
# emoji (.webm). Install on Windows with: winget install Gyan.FFmpeg

requests>=2.32          # Telegram Bot API calls (build_pack.py, check_all_packs.py)
Pillow>=12.3.0          # image decode / 100x100 fitting
numpy>=1.26             # image signature matching (coins/remap_ids.py)
# SVG -> RGBA PNG. resvg is a self-contained Rust renderer shipped as a wheel:
# no system cairo, no build toolchain, and it renders gradients (the previous
# svglib/reportlab backend produced a blank image for those, and reportlab's
# bundled renderPM C backend was removed upstream in reportlab 5.0).
resvg-py>=0.4           # note: the import name is resvg_py
